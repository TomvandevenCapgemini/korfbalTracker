generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Match {
  id        Int     @id @default(autoincrement())
  date      String
  homeTeam  String
  awayTeam  String
  homeScore Int
  awayScore Int
}
